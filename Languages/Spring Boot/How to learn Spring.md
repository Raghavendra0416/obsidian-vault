1. What is Spring & Spring Boot? Architecture of Spring Boot? History of Spring & Spring boot? What are the specifications of Spring Boot?
2. What is DI? How do we use DI?  Types of DI? Uses & Examples?
3. What is IOC? What is Spring Container? What are Java Beans? What is a resource? In how many types we can use IOC? What is Configuration Meta Data(& types) ? Example & Uses?
4. Create a java-spring boot project.
	1. Create the Project Using DI & IOC. Both should be used at same time to achieve loose coupling.
	2. Create for DI using Setter & Constructor.
	3. What are the types of methods(for primitive data types, collections, reference(obj)) we follow while declaring - **Setter DI**? 
	4. What types of methods we follow while using - **Constructor DI**? 
	5. How is Constructor DI is different from Setter DI?
	6. Uses? Limitations? Examples? Real Life Examples?